console.log('Hello Console');

var app = {};

app.config = {
  sessionToken: false,
};

// Ajax cleint
//
//
app.client = {};

app.client.request = function (
  headers,
  path,
  method,
  queryString,
  payload,
  callback
) {};
